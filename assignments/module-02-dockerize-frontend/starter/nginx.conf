server {
    # TODO 1: Listen on port 80
    listen ____;
    server_name _;

    # TODO 2: Set root directory for static files
    root ____;
    index index.html;

    # TODO 3: Configure SPA routing (try_files)
    location / {
        ____
    }

    # TODO 4: Configure API proxy to backend
    location /api/ {
        proxy_pass ____;
    }
}
